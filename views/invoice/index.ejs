<!DOCTYPE html>
<html lang="en">
<!-- Head -->
<%- include('../partials/head.ejs') %> 

<body>
    <% if(invoices.length> 0){ %>
        <%invoices.forEach(invoice=> {%>
            <p>
                <%= invoice.body %>
            </p>
            <% })} else{ %>
                <p>No invoice yet</p>
                <% } %>
</body>
</html>